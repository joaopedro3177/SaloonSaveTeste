# Saloon Save

Estrutura inicial organizada para o TCC:

- `frontend/index.html`: landing page
- `frontend/form.html`: acesso e cadastro com entrada temporaria na dashboard
- `frontend/dashboard/`: paginas internas do sistema
- `frontend/assets/`: CSS, JS e imagens compartilhadas
- `backend/src/`: base separada para API e validacoes futuras

Fluxo atual:

1. Landing page
2. Formulario
3. Dashboard liberada sem autenticacao real

Quando o back-end estiver pronto, o proximo passo natural e substituir o redirecionamento mock do formulario pela validacao real da API.
